interface RecentActivityProps {
  viewOnly?: boolean;
}

const RecentActivity = ({ viewOnly }: RecentActivityProps) => {
  return <div>Hello I am the recent activyt</div>;
};

export default RecentActivity;
